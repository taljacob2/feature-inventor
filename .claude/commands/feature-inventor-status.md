---
description: Show feature-inventor's current status (up next, recently shipped, recent attempts)
---

Run `node dist/cli.js status` from the feature-inventor repo root. If `dist/` is missing or looks
stale, run `npm run build` first. Present the output clearly to the user rather than dumping raw
text verbatim if you can make it more readable.
