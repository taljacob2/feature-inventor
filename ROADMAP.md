# Roadmap

This file is regenerated every run — never treat it as final. Items move
Now → Next → Later as they get picked up, and Horizon always gains at least
one new entry per run so the backlog never visibly empties (see
`VISION.md` operating principle #3).

Format: `- [ ] Title — effort/value, why it matters`. Older items still use
rough t-shirt sizes (S/M/L); `workflows/nightly.js` itself now scores
candidates with real ICE (Impact/Confidence/Ease) per `RESEARCH.md` §3 —
new items below use ICE where the estimate was made after that switch.

Items marked `[x]` are done — see `CHANGELOG.md` for the full story on each
(tagged `loop-shipped` or `hand-built`, so you can tell which were built
autonomously). Items that predate CHANGELOG.md itself (initial v0
scaffolding) aren't in there; see `git log` for that history instead.

## Now (this run's candidates, cheapest first)

- [x] `feature-inventor status` CLI command — done, v0 scaffold.
- [x] Sanity-check harness (`npm test` via vitest) — done, v0 scaffold.
- [x] Switch Research/Prioritize scoring in `workflows/nightly.js` from ad-hoc S/M/L to real ICE (Impact/Confidence/Ease) — done, hand-built; ranking is computed deterministically in code from ICE scores, the Prioritize agent only dedupes/scope-filters, see `RESEARCH.md` §3.
- [x] Independent verification pass before trusting a "shipped" claim — done, hand-built; a second agent independently re-runs tests and inspects the diff, reverting via `git revert` if it doesn't hold up, mitigating evaluator overconfidence per `RESEARCH.md` §2, §4 — and this first real run proved its worth by catching a false "matched" confidence claim on the backlog-counts feature below and reverting it before the bad claim could compound.
- [x] Clean error + non-zero exit in `cli.ts` when ROADMAP.md/CHANGELOG.md are missing or malformed — shipped, commit `7c4de84`.
- [x] Structured feature-attempt log persisted to a file — shipped this run, commit `a6ac44d`; `feature-inventor status` now reads it back as a "Recent feature attempts" section.
- [x] README.md quickstart — shipped this run, commit `8d45988`.
- [x] `status --json` machine-readable output mode — shipped this run, commit `a7f8887`.
- [x] Bug fix: `workflows/nightly.js`'s Finalize step now tracks the Implement loop's actual break point (`attemptedCount`/`stopReason`) instead of deriving "candidates not attempted" solely from `orderedFeatures.slice(queue.length)` — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] Retry (fixed): backlog section counts (Next/Later/Horizon) in `status`, with `parseSection`'s heading match now requiring a line-end or parenthetical separator after the section name instead of a bare `\b` word boundary, plus a fixture with a colliding `## Next Steps` heading — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] `feature-inventor stop`/`feature-inventor recap` CLI commands, a graceful stop-flag check in `workflows/nightly.js`, three slash commands, and a fix for `appendFeatureLogEntry`'s Workflow-script filesystem-access bug — see `CHANGELOG.md` 2026-08-01 (hand-built), commit `c27022a`.
- [x] Removed the hardcoded machine-specific absolute path from `workflows/nightly.js`'s `REPO_ROOT` (and made `BRANCH_NAME` configurable too) — see `CHANGELOG.md` 2026-08-01 "Remove hardcoded repo path..." (hand-built).
- [x] `--help`/`-h` and `--version`/`-v` flags on the CLI — ICE 5/8/9 (7.33) — shipped this run, commit `d1f36a3`; verification found no functional issues, only that the new switch-case routing itself (vs. the exported helpers) has no automated test — an existing pattern gap, not a regression.
- [x] Show upcoming Next-section titles in `status` when Now is empty — ICE 7/7/8 (7.33) — shipped this run, commit `4e9f2e68318a995811928e0ec956f97bfa54d9e6`; clean, no material verification concerns.
- [x] `feature-inventor recap --json` — ICE 4/8/9 (7.0) — shipped this run, commit `b72ce3d4ca533c6c3a065dd53904fac57a85f5c0`; clean, no material verification concerns.

## Next

- [x] Retrospective self-assessment fields on every `feature-log.jsonl` entry — implemented as an optional `selfAssessment` block (creativity/difficulty/confident/wantedHumanGuidance/knowledgeGaps/modelFit), collected via `IMPLEMENT_SCHEMA` on every attempt regardless of outcome — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] Calibration log: predicted ICE confidence vs. actual outcome, plus a hallucination-rate metric — implemented as `src/calibration.ts`'s `computeCalibrationStats()`, surfaced in `status`/`--json`, and referenced in `workflows/nightly.js`'s Finalize reflection step — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] Nightly summary push notification on run completion — implemented as a delegated `sendRunCompletionNotification()` in `workflows/nightly.js`'s Finalize phase — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] Live per-feature progress visibility during a run — `workflows/nightly.js`'s Implement loop now calls `TaskCreate`/`TaskUpdate` (via delegated agents) so `TaskList`/`TaskGet` show real-time per-feature status — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] Explicit "where did we stop" / resume-point summary — implemented as `src/run-summary.ts` (a gitignored `.feature-inventor-last-run.json`, always overwritten with just the most recent run), written by `workflows/nightly.js` at Finalize and surfaced as a new "Last run" section in `status`/`--json` — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [ ] `feature-inventor doctor` diagnostic/pre-flight command — ICE 7/6/7 (composite 6.67) — carried forward, not attempted this run (queue was cut short by `max-features-reached` before reaching it).
- [ ] Non-mutating `--planOnly` / dry-run mode for `workflows/nightly.js` — ICE 8/5/5 (composite 6.0) — carried forward, not attempted this run.
- [ ] Per-run markdown "run report" (job-summary style) — ICE 6/5/6 (composite 5.67) — carried forward, not attempted this run.

## Later (harder, uncertain effort)

- [ ] `CronCreate` wiring for unattended nightly runs — ICE 9/4/4 (composite 5.67) — carried forward again this run, still gated on a human reviewing a few manual dry runs first (see `VISION.md`'s harness-not-dark-factory section). Correction: despite CHANGELOG.md/ROADMAP.md prose describing "this run" as the loop's first execution, no run had (as of 2026-08-01) actually gone through the Workflow tool or `CronCreate` — confirmed via `TaskList`/`CronList` (both empty) and the absence of `feature-log.jsonl` (an automated run's delegated agents would have created it). The commits so far are hand-built, not loop-shipped. Still need a genuine `workflows/nightly.js` dry run through the Workflow tool before wiring cron. **Not picked up again this run** — this item's own gate (a reviewed manual dry run first) hasn't been satisfied, so it stays deliberately skipped rather than forced through.
- [x] Output-mode switch (auto-commit / PR-per-feature / batched summary) — implemented as `args.outputMode` in `workflows/nightly.js`, defaulting to `"auto-commit"` (the only mode built); other values error out explicitly rather than silently no-op'ing, since `"pr-per-feature"` needs a human decision on the never-push boundary first — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] Per-feature "harness vs. dark factory" autonomy score — implemented as `src/autonomy.ts`'s `computeAutonomyScore()`, a *deterministic formula* (not yet a trained model — there still isn't enough history for that) over `selfAssessment.knowledgeGaps`/`difficulty`/`confident`/`wantedHumanGuidance` and a new factual `filesChanged` count from independent verification's `git show --stat`, surfaced inline in `status`'s "Recent feature attempts" — see `CHANGELOG.md` 2026-08-01 (hand-built). Training a real model against outcomes remains a Later item once there's enough logged history.
- [x] Audit other regex-based parsing in `src/roadmap.ts` — found and fixed the same class of over-eager boundary match in `parseChangelogEntries` (matched any `## ` line, not just date-prefixed entries); `stripFencedCodeBlocks` and the bullet-line regex reviewed and found sound — see `CHANGELOG.md` 2026-08-01 (hand-built).
- [x] Parallelize independent features in the Implement phase, scored by a pairwise "feature collision rate" — implemented the *scoring and ordering* half only: `workflows/nightly.js` now estimates pairwise collision rates during Prioritize and orders the queue by ICE tier then lowest predicted collision within a tier. Actual parallel execution (via the `Workflow` tool's `isolation: 'worktree'` option) is deliberately NOT built — the item's own text says the parallelize-vs-sequential thresholds need real calibration data this project doesn't have yet, so the Implement loop stays strictly sequential this pass — see `CHANGELOG.md` 2026-08-01 (hand-built).

## Horizon (speculative — proof this list never runs dry)

- [ ] Support targeting a repo other than this one — deliberately NOT designed in from day one (see VISION.md); revisit once the self-hosted loop has proven itself and this stops being speculative. Design note from 2026-08-01: vendoring feature-inventor into a target repo via `git subtree` or a submodule is the tempting first idea but has a real collision problem — the loop's own conventions write to root-level `ROADMAP.md`/`CHANGELOG.md`/`VISION.md`, which would clash with a target repo's own files of the same name; a submodule only sidesteps that if feature-inventor's state also moves into its own namespaced subdirectory (e.g. `.feature-inventor/roadmap.md`) rather than the repo root. The more promising direction, consistent with `VISION.md`'s own "swappable runtime adapter" principle: ship feature-inventor as an installable CLI/package with an `init` step that scaffolds its own namespaced state files and slash commands into a *consuming* repo, using `nightly.js`'s already-existing `args.repoRoot`/`args.branchName` as the seam for "which repo/branch does this run operate on" — rather than vendoring feature-inventor's source into that repo at all. Out-of-the-box, a user would then: install the package, run an init step once per repo they want it managing (one or several), and get a dedicated branch plus namespaced roadmap/changelog the loop commits to — same harness-not-dark-factory boundary as this repo, just parameterized instead of hardcoded to "this repo."
- [ ] A small dashboard (even just a rendered markdown/HTML view) instead of CLI-only status, once there's a live run worth visualizing (see `RESEARCH.md` §5 on progress-display UX patterns) — `status --json` shipped this run gives this a real data source to render instead of scraping CLI text. Once the retrospective self-assessment fields above exist, this is also where creativity/difficulty distributions, the hallucination-vs-good ratio, confidence trends, and model-fit assessments would actually get visualized as live-updating graphs rather than buried in `feature-log.jsonl`.
- [ ] Explore a hosted-service pivot per `VISION.md`'s runtime-adapter section (Claude Agent SDK instead of native Claude Code automation) — purely speculative, not to be picked up until the self-hosted loop has run unattended for a meaningful stretch.

---
*Last regenerated: 2026-08-01, after a second same-day run: 3 more features shipped and independently verified (`--help`/`--version` flags, `status` Next-preview fallback, `recap --json`), 0 abandoned or reverted — see `Now` above for commit shas. Run stopped at `max-features-reached` with 4 already-ICE-scored candidates left in the queue (`doctor`, `--planOnly` dry-run, `CronCreate` wiring, run-report); all four are carried forward into `Next`/`Later` below with their existing scores rather than dropped. Re-evaluation note: all 3 shipped items this run had ICE ease ≥ 8 and were self-rated "difficulty: easy" / "modelFit: overkill" — the composite-ICE ordering is (correctly, so far — hallucination rate is still 0 across all 3 logged attempts) front-loading the cheapest wins, but that also means the higher-impact, lower-ease items (`doctor` 7/6/7, `--planOnly` 8/5/5, `CronCreate` 9/4/4) keep losing the ranking run after run and have now gone two runs without being attempted. Nothing here indicates any individual Confidence/Ease estimate was wrong — no reverts, no verification-flagged false claims — so no change to how estimates are made. But if this pattern continues for another run or two, consider a tie-breaker (e.g. bumping a candidate's priority after N consecutive runs unattempted) so genuinely higher-impact work isn't perpetually starved by easier items.*
